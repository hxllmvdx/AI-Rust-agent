[project]
name = "frontend"
version = "0.1.0"
description = "Streamlit frontend for the Rust AI agent"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.44",
    "requests>=2.32",
]

[tool.uv]
package = false
